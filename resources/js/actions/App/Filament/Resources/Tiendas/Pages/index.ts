import ListTiendas from './ListTiendas'
import CreateTienda from './CreateTienda'
import EditTienda from './EditTienda'

const Pages = {
    ListTiendas: Object.assign(ListTiendas, ListTiendas),
    CreateTienda: Object.assign(CreateTienda, CreateTienda),
    EditTienda: Object.assign(EditTienda, EditTienda),
}

export default Pages