import ListCategorias from './ListCategorias'
import CreateCategoria from './CreateCategoria'
import EditCategoria from './EditCategoria'
const Pages = {
    ListCategorias: Object.assign(ListCategorias, ListCategorias),
CreateCategoria: Object.assign(CreateCategoria, CreateCategoria),
EditCategoria: Object.assign(EditCategoria, EditCategoria),
}

export default Pages