<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirigir automáticamente todo el tráfico a la subcarpeta public/
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
